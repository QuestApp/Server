{% extends 'QuestServerQuestBundle:QuestionType:layout.html.twig' %}

{% block content -%}
    <h1>QuestionType list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Icon</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_questiontype_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.name }}</td>
                <td>{{ entity.icon }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_questiontype_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_questiontype_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_questiontype_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
