{% extends 'QuestServerQuestBundle:QuestionType:layout.html.twig' %}

{% block content -%}
    <h1>QuestionType edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_questiontype') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
